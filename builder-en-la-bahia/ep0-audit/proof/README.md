# Craft demo (optional)

`craft-demo-after-no-reshoot.mp4` — **33.5s**, 720×1280, ~3.9 MB.

**What it shows (edit-only, no new camera):**
- Cold open (Ken Burns intro deleted)
- ~1.45× punch-in on take2 (pants + burned-in captions cropped off)
- Captions rebuilt from the SRT **with word spaces**
- Lower-third + employer chip row

**What it does not show:** a publishable episode. Same kitchen lock-off, same hat, same 30s of CV. That is the ceiling.

Rebuild (needs the review720 path):

```bash
./build_craft_demo.sh /path/to/ep0_v4_builder_en_la_bahia_review720.mp4
```

`_work/` is local only — do not commit it.
